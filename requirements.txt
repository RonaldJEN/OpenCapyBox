# FastAPI 后端依赖

# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.6

# 数据库
sqlalchemy>=2.0.23
alembic>=1.12.1
pydantic>=2.8.0
# 配置管理
pydantic-settings>=2.1.0
python-dotenv>=1.0.1

# CORS
fastapi-cors>=0.0.6

# 工具
python-jose[cryptography]>=3.3.0  # JWT（未来用）
passlib[bcrypt]>=1.7.4  # 密码哈希（未来用）

# ========== OpenCapyBox 核心依赖 ==========
# 后端通过 sys.path 导入 mini_agent 源代码
# 需要安装 mini_agent 的依赖，但不需要安装 mini_agent 包本身

# HTTP 客户端（LLM API 调用）
httpx>=0.27.0

# LLM SDK
anthropic>=0.39.0
openai>=1.50.0

# Token 计数
tiktoken>=0.5.0

# 配置文件
pyyaml>=6.0.1

pyjwt>=2.8.0,<2.9.0

# 网络请求
requests>=2.31.0

# 搜索工具（可选）
zhipuai>=2.0.1  # 智谱 AI 搜索工具

# ========== 测试相关依赖 ==========
# 測試依賴
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-html>=4.1.0
pytest-json-report>=1.5.0
coverage-badge>=1.1.0
opensandbox>=0.1.0

# 调度
apscheduler>=3.10.0
